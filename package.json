{
  "name": "stripe-setupintents-example",
  "version": "1.0.0",
  "description": "Node/Express + Stripe SetupIntents example for saving cards for off-session charges.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "stripe": "^17.5.0"
  }
}
